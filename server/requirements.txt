# MCP server SDK — requires >=1.6.0 for Streamable HTTP transport (spec 2025-03-26)
mcp[server]>=1.9.0

# ASGI server
uvicorn[standard]>=0.30.0

# HTTP client for OAuth proxy (token endpoint relay)
httpx>=0.27.0

# AWS SDK
boto3>=1.34.0

# JWT verification (RS256 + JWKS)
pyjwt[cryptography]>=2.8.0
