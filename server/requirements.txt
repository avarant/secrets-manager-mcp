# MCP server SDK — requires >=1.6.0 for Streamable HTTP transport (spec 2025-03-26)
mcp[server]>=1.6.0

# ASGI adapter for AWS Lambda + API Gateway HTTP API v2
mangum>=0.19.0

# AWS SDK
boto3>=1.34.0
